# insertion-sort-

# Approach
 i used two counter variables

 iterates through the array starting at index 1

j  starts at i - 1 and moves backward through the already-sorted portion
shifting elements one position to the right as long as they are greater than the current element being inserted
Once no more elements need to shift the current element is placed at position j + 1.

